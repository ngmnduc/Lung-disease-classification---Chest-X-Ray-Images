# --- FILE RÁC HỆ THỐNG (Giữ nguyên) ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.DS_Store
Thumbs.db
desktop.ini

# --- MÔI TRƯỜNG ẢO (Giữ nguyên) ---
venv/
env/
ENV/
.venv/
.env
.vscode/
.idea/

# --- FILE DỮ LIỆU NẶNG (QUAN TRỌNG: CHẶN CÁI NÀY) ---
# Chặn toàn bộ file numpy (X_train nặng 4GB) ở bất cứ đâu
*.npy

# Chặn thư mục chứa ảnh gốc tải về
data/chest_xray/
data/raw/

# Chặn thư mục processed (chứa ảnh nén dạng numpy)
data/processed_data/

# --- FILE MODEL & KẾT QUẢ (QUAN TRỌNG: CHO PHÉP CÁI NÀY) ---
# Tuyệt đối KHÔNG chặn *.pkl vì cần đẩy model lên cho Streamlit
# Nếu có dòng *.pkl ở đây thì xóa đi nhé!

# Chặn file model quá nặng (nếu có dùng deep learning h5)
*.h5
*.weights

# --- LOGS & KHÁC ---
*.log
.ipynb_checkpoints/
*.ipynb